# Rustlings

My Rust learning journey. Working through the [Rustlings](https://github.com/rust-lang/rustlings) exercises one at a time.

## How it works

Each exercise in `exercises/` has a `todo!()` or broken code. I fix it so it compiles and runs. Solutions are in `solutions/` for when I get stuck.
